# Finanzplaner-PWA

Private Ein-Nutzer-Finanz-App (AUD, wöchentlicher Rhythmus). Kein Backend, keine Auth. Ziel: installierte PWA auf iPhone + MacBook.
Plan & Phasenstatus: `docs/PLAN.md` – **nur an der freigegebenen Phase arbeiten**, Checkboxen dort pflegen.

## Stack

Vite 8 · React 19 · **TypeScript ~6.0 (gepinnt – TS 7 erst, wenn typescript-eslint es unterstützt; kein `baseUrl`)** · Tailwind v4 (CSS-first, Tokens in `src/styles/tokens.css`) ·
shadcn/ui (Stil `radix-nova`, `src/shared/ui`) · Zustand (nur UI-State) · Dexie 4 + dexie-react-hooks · Recharts 3 (+ react-is) · date-fns 4 · motion (`motion/react`) ·
sonner · lucide-react · zod · vite-plugin-pwa · Vitest + fake-indexeddb + Testing Library · puppeteer-core (nur `e2e/`, nutzt das lokal installierte Chrome) · react-router 8 (`react-router`, `react-router/dom`) ·
ESLint 10 + typescript-eslint (nicht Oxlint – wir brauchen `no-restricted-syntax`/-`imports` pro Ordner) · Prettier + `prettier-plugin-tailwindcss` ·
Deployment: Vercel (statisch, Projekt `finance-planner` im Team `jonasworkings-projects`, per Git mit `Jonasworking/finance-planner` verbunden: Push auf `main` = Production, jeder andere Branch = Preview; `vercel.json` enthält den SPA-Rewrite; der Vercel-Login-Schutz ist auf Wunsch des Nutzers aus, alle Deployment-URLs sind öffentlich – Einstellungen am Vercel-Projekt nur nach Rückfrage ändern). Paketmanager: npm. Kein `framer-motion`, kein `react-router-dom`, kein `next-themes` (eigener `themeStore`).
`vaul` kommt nur indirekt über den shadcn-Drawer (Radix-Basis nutzt weiterhin vaul) und wird ausschließlich in `shared/components/ResponsiveSheet.tsx` verwendet → dort austauschbar.
Pakete werden pro Phase installiert (Dexie/zod/date-fns → Phase 1, Recharts → Phase 4, vite-plugin-pwa → Phase 6).

## Befehle

`npm run dev` · `npm run build` · `npm run typecheck` · `npm run lint` · `npm run test` (einzeln: `npx vitest run src/lib/money.test.ts`) · `npm run test:coverage` (Schwellen für `src/lib`: 90 % Zeilen/Funktionen, 80 % Branches) · `npm run format` · `npm run check:bundle` (nach `npm run build`: initiales JS ≤ 250 KB gzip, listet die Start-Chunks)
**Jeder Test läuft zweimal** – als Vitest-Projekt `sydney` und `berlin` (`TZ` gesetzt): Kalenderlogik darf weder vom UTC-Offset noch von DST abhängen. Nur eine Zone: `npx vitest run --project sydney`.
Neue shadcn-Komponente: `npx shadcn@latest add <name>` → danach **immer** `npm run ui:fix-imports` (sonst schlägt der Lint fehl, siehe `cn`-Regel).
**`npm run test:e2e`** – Browser-Smoke-Suite (`e2e/`, `node:test` + puppeteer-core, ~90 s): baut das Production-Bundle, startet `vite preview` auf Port 4178 und spielt Onboarding, Ausgabe erfassen, Wisch-Löschen mit Undo, Wochenabschluss (laufende Woche + Warteschlange) Topf anlegen → einzahlen → aus dem Topf bezahlen, Budget ändern, die Analyse (Charts gezeichnet, Monatsansicht, Tabelle, Drilldown, EUR), Task anlegen → abhaken → rückgängig (inkl. Home-Widget), Insight-Karte wegwischen, den Was-wäre-wenn-Rechner (Regler per Drag und Pfeiltasten, Kurve, als Budget übernehmen) und die Breitenprüfung der Karten in echtem Chrome durch (390×844 Touch + 1440×900). **Nicht Teil des Gates** – laufen lassen vor der Abnahme jeder UI-Phase und nach Änderungen an Layout, Gesten oder Sheets. `E2E_BASE_URL=<url>` testet stattdessen einen laufenden Server (Dev-Server, Vercel-Preview oder Production – jede Journey nutzt einen frischen Browser-Kontext, echte Daten bleiben unberührt) · `CHROME_PATH` für ein anderes Chrome · `E2E_HEADFUL=1` zum Zuschauen · Screenshot bei Fehlschlag in `e2e/artifacts/`.

## Architektur – wo liegt was

- `src/lib` – **gesamte Fachlogik als reine Funktionen**. Kein React, kein Dexie, kein `Date.now()`/`new Date()` ohne Parameter (`today`/`now` reinreichen). Jede Funktion hat Tests (`*.test.ts` daneben). **Die Domänen-Typen liegen hier** (`lib/types.ts`, inkl. `SCHEMA_VERSION`, `PRIMARY_POT_ID`), weil `lib` nicht aus `db` importieren darf.
  Module: `money` (auch `MoneyDisplay`/`formatMoney`/`formatPercent`, Achsen-Einheiten, EUR-Kurs parsen) · `dates` · `ids` · `expenses` · `budget` (`runningWeekBudget`, `budgetUsage` inkl. „reserviert", `dueBudgetWarnings`) · `pots` (`summarizePot(s)`, `potHistory`, `availableForExpense`) · `recurrence` · `savings` (`summarizeWeek`, Topfstände, `pendingWeeks`) · `streak` · `forecast` · `whatif` · `analytics` (`buildAnalytics` = alles, was der Analyse-Screen zeigt; `analyticsWeeks`, `flowSeries`, `latestComparison`, `foldSlices`, `categoryDetail`) · `dashboard` (`nextStep`, `projectWeek`, `weekProgress`) · `amountInput` (Numpad-Zustandsmaschine) · `tags` (Normalisierung, Autocomplete) · `insights` (strukturiert: `kind` + Daten, Text rendert die UI) · `tasks` (`dueState`/`isOverdue`, `splitTasks`, `tasksForDashboard`, `describeDue`/`describeDone`; Sortierung deterministisch bis zum `id`-Tiebreaker) · `ledger` (`checkLedgerInvariants`) · `csv` · `backup` (zod-Schema, per Typ-Assertion an `AppData` gekoppelt).
- `src/db` – Dexie-Schema (`schema.ts`), Seeds (`seed.ts`), Repos (`repos/`), `queries.ts` (`loadAppData`), `demo.ts` (12 Demo-Wochen, nur Dev/Tests). **Einziger Ort mit Dexie-Schreibzugriff**; Mehr-Tabellen-Operationen in einer `rw`-Transaktion über `ledgerTables`, darin nur Dexie-`await`s.
  Schreib-API: `repos.expenses.add/update/remove/restore` · `repos.weeks.setIncome/close/reopen` · `repos.pots.create/update/archive/unarchive/deposit/withdraw/transfer/removeTransaction/restoreTransaction` · `repos.recurring.create/update/remove/restore/materialize(today)` · `repos.onboarding.complete(input, today)` (eine Transaktion, idempotent; Startguthaben hat die feste ID `opening-balance`) · `repos.budgets.set(today, …)` · `repos.categories` · `repos.tasks.add/update/setDone/remove/restore` (Titel getrimmt und nie leer → `invalid-title`; `linkedPotId` muss existieren → `unknown-pot`, archiviert erlaubt; `setDone` idempotent) · `repos.settings` · `repos.backup.export/import/restoreSafetyCopy/wipeAll`. Regelverstöße werfen `DomainError` mit `code` (UI übersetzt den Code).
  **Nadelöhr:** jede Ausgaben-Mutation endet in `writeExpense` (`db/repos/context.ts`) → Funding-Spiegel `fund:<id>` + `syncWeekDerived` für alte UND neue Woche. Nie an `db.expenses` vorbei schreiben.
  Tests: `createRepos(new FinanceDB('name'), fakeClock)` gegen fake-indexeddb; `afterEach` prüft `checkLedgerInvariants(await loadAppData(db))` – bei neuen Repo-Tests beibehalten.
- **Start-Chunk < 250 KB gzip** (initiales JS, gemessen über alle statisch geladenen Chunks): nur Home ist im Start. Routen-Seiten liegen in `features/<name>/pages.ts` und werden in `app/router.tsx` per `lazyPage(title, …)` geladen; Sheets, die `AppShell` einbindet, liegen in `features/<name>/sheets.ts` und kommen per `lazy()` direkt nach dem ersten Bild. `index.ts` enthält nur, was statisch gebraucht wird – **nie eine Seite oder ein Shell-Sheet dort re-exportieren**, sonst landet es wieder im Start-Chunk. Animationen: **`m.*` statt `motion.*`** (ESLint), die Features (`domMax`) lädt `LazyMotion` in `app/providers.tsx` nach; Komponententests mit Drag/Animation rendern in `<MotionFeatures>` aus `src/test/ui.ts`.
- `src/features/<name>` – UI + Hooks. Ein `useLiveQuery`-Querier pro Screen (Lese-Funktionen in `db/queries.ts`: `loadDashboard`, `loadCloseWeek`, `loadRecurring`, `loadExpensesOfWeek`, `loadExpenseFormData`, `loadCategories`, `loadBudget`, `loadPots`, `loadAnalytics`, `loadTasks`); `undefined` = lädt, `null` = nicht gefunden. Cross-Feature-Importe nur über `index.ts`.
  Liefert eine Abfrage zu einer wechselnden ID (z. B. Bearbeiten-Sheet), gehört die ID ins Ergebnis – `useLiveQuery` hält nach dem Wechsel kurz die alte Antwort.
  `repos.backup` wird lazy geladen (zod + Ledger ≈ 28 KB gzip in eigenem Chunk) – Backup-Code nie statisch in den Startpfad importieren.
- `src/shared` – `ui/` (shadcn, generiert), `components/`, `hooks/`, `stores/`, `lib/utils.ts`. Importiert nie aus `features`. Darf `src/lib` importieren.
- `src/app` – Router, Provider, Shell (`AppShell`, `BottomTabs`, `Sidebar`, `MorePage`), Dev-Styleguide `/dev/tokens` (nur Dev-Build). Features importieren nie aus `app`.
- Die Schichtenregeln sind in `eslint.config.js` erzwungen (inkl. Datums-/Uhr-Verbote); Ordner-Configs **ersetzen** die Regel, daher neue Verbote dort überall ergänzen.
- **`cn` immer aus `@/shared/lib/utils`** – nie das nackte Paket `"cn"`: nur die konfigurierte Variante kennt unsere Schriftgrößen (`text-display|h1|h2|body|label|caption`); sonst verwirft der Merger sie neben Textfarben. Neue `--text-*`-Tokens dort nachtragen (Test: `shared/lib/utils.test.ts`).
- Seiten nutzen `<Page title=…>` (Sticky-Glas-Header + Content-Spalte); Buttons auf Touch-Screens `size="touch"` / `"icon-touch"` (44 px).
- Shared-Bausteine: `Money` (mit `display` auch in EUR – nur Screens mit Währungsumschalter reichen es durch) · `SegmentedControl` (Eins-aus-n-Schalter; der Indikator gleitet in der GRUPPE, nicht im Button – sonst ragt er während der Fahrt aus dem Button) · `GlassCard` · `ProgressRing` (animiert; `reserved` = blasserer Bogen für Verplantes) · `ProgressBar` · `AmountSlider` (Beträge per Regler, 44-px-Griff – **nicht** den generierten shadcn-`Slider` nehmen, dessen Griff ist 12 px) · `ColorPicker`/`IconPicker` · `ResponsiveSheet` · `Numpad` (Betrag ist Text, kein `<input>`; Tasten auf `pointerdown`) · `SwipeRow` (Wisch = löschen, kurzer Wisch = Button zeigen; **schluckt den Klick nach einem Drag** – sonst öffnet der Wisch die Zeile) · `TagInput` · `CategoryIcon` + `shared/lib/categoryStyle.ts` (Icon-Registry und Farbklassen als vollständige Strings – nie `bg-${color}` bauen) · `errorMessage(error)` für `DomainError`-Codes.
- Sheets mit Formular: Formular per „Session"-Zähler keyen, der beim **Öffnen** hochzählt (`uiStore.quickAddSession`/`editSession`) – nicht am `open`-Flag, sonst springt der Inhalt während der Schließ-Animation. Ein Sheet ohne Inhalt nie offen lassen: ein Modal-Overlay blockiert alles darunter, auch den Undo-Toast.
- Ablauf der App: `AppShell` zeigt bis `settings.onboardingDone` nur `OnboardingFlow`; danach bucht `useMaterializeRecurring(today)` fällige Daueraufträge bei App-Start und Tageswechsel. Wochenabschluss, -bearbeitung und „wieder öffnen" laufen über `CloseWeekSheet` (`uiStore.openCloseWeek(weekStart)`); offene Wochen kommen aus `lib/savings.pendingWeeks` (älteste zuerst).
- **Eine Budget-Rechnung für alle:** Hero-Ring, Budget-Seite und Warn-Toasts nehmen ihre Zahlen aus `lib/budget.runningWeekBudget` (ausgegeben + noch nicht gebuchte Daueraufträge der Woche) – nie an einer Stelle selbst `spent/limit` rechnen, sonst widersprechen sich Ring und Warnung. Warnungen (`features/budget/useBudgetWarnings`, in `AppShell` gemountet) feuern genau einmal je Schwelle und Woche, weil sie ein Protokoll des bereits Gemeldeten führen (`localStorage` `fp.budgetWarnings`, pro Gerät, bewusst nicht in der DB/im Backup) statt Vorher/Nachher zu vergleichen.
- **Formular-Hinweise nutzen die Repo-Regeln:** Betragsprüfungen im UI (`PotMoveSheet`, „Bezahlt aus" im `ExpenseForm`) rufen `validateWithdrawal`/`validateTransfer`/`availableForExpense` aus `lib` auf – dieselben Funktionen wie das Repo –, damit Hinweis und Schreibzugriff nie auseinanderlaufen.
- **Analyse & Charts** (`features/analytics`, Route lazy über `app/routes/AnalyticsRoute`): Vor Chart-Code den `dataviz`-Skill laden – erst die Form, Farbe zuletzt, Farben mit dessen Validator prüfen. **Recharts darf nur in `features/<name>/charts/` importiert werden** (ESLint), die Karten laden den Ordner per `lazy()` → eigener Chunk (Recharts selbst landet in einem gemeinsamen Chunk). `ChartCard`, `ChartTooltip` (`shared/components`) und `shared/lib/chartTheme.ts` sind Recharts-frei und werden von Analyse und Was-wäre-wenn geteilt. Was ein Chart zeichnet, entsteht als Zeilen in `chartData.ts` (ohne Recharts, dort getestet); Zahlen kommen ausschließlich aus `lib/buildAnalytics`. Jede Karte nutzt `ChartCard` (feste Plot-Höhe inkl. Achse, Legende in Textfarbe, **Tabellen-Zwilling**, Selbsterklärung ohne Daten, `role="group"` + `aria-label="<Titel>: Chart"`) und `ChartTooltip` (Werte führen, Serien als Strich). Marken: ≤ 24 px breit, Rundung nur am Daten-Ende, 2-px-Lücke in Flächenfarbe statt Rahmen, nie zwei y-Achsen, Text nie in Serienfarbe. **Flächen tragen `--chart-*`-Tokens** (dunklere Stufen derselben Farbtöne – die Markenfarben sind als Fläche auf Dunkel zu hell); Kategoriefarben über `chartColor()` aus `categoryStyle.ts`, und **eine Kategorie nie nur über Farbe** kenntlich machen (neun Farbtöne sind nicht paarweise unterscheidbar). Offene Wochen zählen in keine Summe (Einkommen fehlt) und erscheinen blass; Monate = Ø pro abgeschlossener Woche. Komponententests ersetzen `./charts` durch eine Klartext-Ausgabe der Zeilen (jsdom hat kein Layout → Recharts zeichnet nichts); das echte SVG prüft die Smoke-Suite mit `waitForChart`.
- EUR ist nur Anzeige: `moneyDisplay(settings)` → `MoneyDisplay`; der Kurs wird im Sheet „EUR-Kurs" gepflegt (`uiStore.openEurRate({ enableOnSave })`, in `AppShell` gemountet), das Repo lehnt unbrauchbare Kurse mit `invalid-rate` ab.
- **Tasks** (`features/tasks`, Route `/tasks`, Widget `TasksCard` auf dem Home-Screen nur mit offenen Tasks): Sheet zum Anlegen/Bearbeiten ist EINMAL in `AppShell` gemountet und läuft über `uiStore.openTask(id?)` (`null` = neu; Session-Zähler wie bei den anderen Sheets). Der Haken (`TaskCheck`) zeigt beim Abhaken erst den Zustand (Spring `bouncy`, gezeichneter Strich) und schreibt **erst nach `CHECK_COMMIT_DELAY_MS`** – mit Live-Queries würde die Zeile sonst springen, bevor man die Animation sieht; Zurücknehmen schreibt sofort. Abhaken und Löschen über `taskActions.ts` (Toast mit Rückgängig). Topf-Kopplung ist nur ein **Verweis mit Fortschritt** (`summarizePot`), es wird nichts gebucht; ein Task darf auf einen archivierten Topf zeigen. Überfällig = offen **und** `dueDate < today` (`lib/tasks.isOverdue`): Coral-Kante links, Label in `text-spent`, Zähler im Seiten-Untertitel und als Badge im Widget. Reihenfolge immer aus `lib/tasks.sortTasks`.
- **Was-wäre-wenn** (`features/whatif`, Route lazy über `app/routes/WhatIfRoute`): alles Fachliche aus `lib/whatif` – `whatIfBase` (Startwert = Summe aller Töpfe, Basis = Ø Gespartes der letzten 8 abgeschlossenen Wochen bzw. Standard-Einkommen − Budget, Regler-Spanne je Kategorie = max(Ø, Limit) in A$5-Schritten, `fromWeek` überspringt eine schon abgeschlossene laufende Woche – ihr Gespartes steckt bereits im Topf), `projectScenario` (Ergebnis + `gainByCategory`), `budgetFromScenario` (Gesamt und Kategorie-Limits minus Kürzung, ohne Limit vom gerundeten Ø aus). Regler und Zieldatum liegen im `whatIfStore` (überleben den Tab-Wechsel, keinen Reload). Die Kurve zeigt standardmäßig den **Vorsprung** über der Basis (bei ~A$1.300 Sparen pro Woche verschwindet eine Kürzung von A$20 auf der vollen Skala), „Gesamt" zeigt beide Stände; keine Zeichen-Animation, `useDeferredValue` für die Punkte – was die Karte SAGT, kommt aus dem nicht verzögerten Szenario. „Als Budget übernehmen" schreibt über `repos.budgets.set(today, …)`, setzt die Regler zurück (sonst würde doppelt gekürzt) und bietet Rückgängig.
- Töpfe: Routen `/pots` und `/pots/:potId` (IDs enthalten `:` → Links nur über `potPath()` aus `shared/lib/routes.ts`). Manuelle Buchungen sind per Wisch löschbar (Undo = `restoreTransaction`), abgeleitete (`auto:`, `fund:`) nicht – eine `fund:`-Zeile öffnet stattdessen ihre Ausgabe.
- **Home-Screen-Regel:** nie leere Karten. Genau EIN nächster Schritt (`lib/dashboard.nextStep`), Prognose ab Tag eins (`projectWeek` mit Standard-Einkommen), Abschnitte ohne Inhalt werden weggelassen oder erklären sich selbst (`WeeksCard` zeigt „So läuft deine Woche", bis eine Woche abgeschlossen ist).
- **Layout-Falle:** Grid-Spalten immer als `minmax(0,1fr)` + `min-w-0` am Grid-Kind – sonst drücken lange `truncate`/`nowrap`-Zeilen die Spalte über den Viewport. `main` schneidet den Überstand ab, deshalb fällt es nicht durch Scrollbalken auf, sondern nur im Bild (Browser-Check: Kartenbreiten messen).
- Seltene Betragsfelder (Einkommen, Budget, Dauerauftrag) nutzen `MoneyInput` (echtes Input mit Dezimal-Tastatur); nur die Schnellerfassung nutzt das `Numpad`.
- In Komponententests `useToday` mocken (`vi.mock('@/shared/hooks/useToday', …)`) und Sheets mit `stubDesktopViewport()` aus `src/test/ui.ts` rendern.
- Löschen fragt nie vorher nach, sondern bietet „Rückgängig" im Toast (`deleteExpenseWithUndo`).
- **Spring-Animationen kennen nur zwei Keyframes** (`animate={{ scale: [1, 1.2, 1] }}` + `type: 'spring'` wirft in jsdom `Only two keyframes currently supported`; im Browser fällt es nicht auf, weil WAAPI es schluckt): Für einen „Pop" den Überschwinger von `spring.bouncy` auf dem Weg zu `1` nutzen, keine Keyframe-Listen.
- Komponenten berechnen nichts Fachliches selbst – fehlt Logik, kommt sie nach `lib` (mit Test). „Heute" kommt aus `useToday()`.

## Domänenregeln

- Geld = Integer-Cents (`…Cents`); Anzeige nur über `formatAUD` / `<Money>` → `A$1.600,00`. EUR nur Anzeige (`Settings.eurRate`).
- Kalendertage = lokale `YYYY-MM-DD`-Strings, nur über `lib/dates.ts`. **Verboten:** `new Date('YYYY-MM-DD')`, `toISOString().slice(0,10)`, Millisekunden-Arithmetik. Wochenstart fix **Montag**; `Week.id` = weekStart.
- Jede Zeile: `id`, `createdAt`, `updatedAt`, `deletedAt` (Soft-Delete; Lesezugriffe filtern Tombstones). Sync-fähig halten.
- Deterministische IDs: `pot:primary`, `cat:<slug>`, `auto:<weekStart>`, `fund:<expenseId>`, `tr:<id>:out|in`, `rec:<recurringId>:<date>`.
- Wochenkennzahlen immer aus Rohdaten (`lib/savings.summarizeWeek`), keine Snapshots. Jede Mutation läuft durchs Repo-Nadelöhr → `syncWeekDerived` für alle betroffenen abgeschlossenen Wochen (alt + neu).
- Topf-finanzierte Ausgaben (`fundedByPotId`) zählen nicht zu Budget, Streak, Wochen-Sparsumme. Topfstand = Summe vorzeichenbehafteter Buchungen.
- Budget ist „gültig ab" (`resolveBudget`), geschrieben wird nur mit `id = aktuelle Woche`. Monats-KPIs = Donnerstags-Regel, nur abgeschlossene Wochen.
- Recurring: `bulkGet`-Filter + `bulkAdd`, nie `bulkPut`. Wipe = `db.delete()` + Reload. Nach Import: `checkLedgerInvariants` + Reload.
- Dexie: ausgelieferte Version nie ändern → neue `version(n+1)` + `upgrade` + Migrationstest; `SCHEMA_VERSION` + `migrateBackup` mitziehen.

## Konventionen

- Komponenten `PascalCase.tsx`, eine pro Datei, Named Exports; Hooks `useXyz.ts`; lib/Repos `camelCase.ts`; Tests `*.test.ts(x)` neben der Quelle.
- Suffixe: `…Page` (Route), `…Sheet` (Bottom-Sheet/Dialog), `…Card`, `…List`/`…Row`. Props-Typ `XyzProps`. Kein `any`, kein Default-Export (außer lazy Routen).
- UI-Texte Deutsch, Code/Kommentare/Commits Englisch.
- **Keine unsichtbaren Sonderzeichen als Literal** (BOM, geschütztes Leerzeichen, U+FFFF …) und keine `\u…`-Escapes über Schreib-Tools – die werden beim Schreiben still in das echte Zeichen verwandelt. Stattdessen `String.fromCharCode(0xfeff)` bzw. `Dexie.minKey/maxKey`. Prüfen: `LC_ALL=C grep -rln $'\xEF\xBB\xBF\|\xC2\xA0\|\xEF\xBF\xBF' src`.
- Gesten testen: echter Pointer-Drag funktioniert in jsdom (`pointerDown` aufs Element, `pointerMove`/`pointerUp` aufs `window`, dazwischen Frames abwarten) – Muster in `shared/components/SwipeRow.test.tsx`. Sheets testen: `window.matchMedia` auf „Desktop" stubben, dann rendert `ResponsiveSheet` den Radix-Dialog – Muster in `EditExpenseSheet.test.tsx`. Neue Absicherungen per Mutation gegenprüfen (Fix kurz entfernen → Test muss rot werden).
- E2E (`e2e/smoke.test.mjs`, Helfer in `e2e/harness.mjs`): nur für Abläufe, die Unit-/RTL-Tests nicht sehen können (echte Gesten, echtes Layout). Jede `journey` startet mit leerer DB (eigener Browser-Kontext) und **fester Uhr: Sonntag 2026-09-20 12:00 in Sydney** – deshalb ist „Diese Woche abschließen" immer im Angebot; erwartete Daten/Beträge darauf beziehen. Selektoren nur über sichtbaren Text und `aria-label`, nie über Tailwind-Klassen. **Keine festen Wartezeiten:** `waitForText`/`waitForModals`/`waitForAmount` nutzen; geklickt wird über `clickText`/`clickSelector` (warten, bis das Ziel stillsteht und wirklich oben liegt). Zwei Fallen: ein schließendes Sheet ist schon `data-state="closed"`, sein Overlay schluckt aber bis zum Ende der Animation jeden Tipp → `waitForModals(page, 0)` wartet, bis es aus dem DOM ist; und der Sheet-Titel steht vor dem Formular (Live-Query) → auf den Inhalt warten, nicht auf den Titel. Charts mit `waitForChart(page, '<Titel>: Chart')` abwarten (Lazy-Chunk + Messung); nicht auf per CSS großgeschriebene Überschriften mit „ß" warten (`innerText` macht daraus „SS"). „Es darf sich kein Sheet öffnen" mit `watchForModals`/`sawModal` prüfen – ein fälschlich geöffnetes Bearbeiten-Sheet schließt sich selbst wieder. Layout immer mit `assertFitsViewport` (misst die Elemente, weil `main` Überstand abschneidet).
- **Phone-Layout immer im echten Browser ansehen (390 px), nicht nur messen:** zu lange Button-Labels laufen über ihre Nachbarn, `truncate` schneidet gern genau die wichtige Hälfte ab („… übrig/drüber"), drei Buttons nebeneinander brauchen Icon über Label. `assertFitsViewport` prüft inzwischen auch überlaufende Button-Texte, aber nicht abgeschnittene.
- **Aria-Labels mit Anführungszeichen** (`„Titel" erledigen`) in Puppeteer/CSS-Selektoren mit einfachen Anführungszeichen schreiben: `button[aria-label='„X" erledigen']` – das gerade `"` im Label bricht sonst den Selektor.
- **Zeilen aus derselben Millisekunde** (`createdAt` gleich, `id` zufällig) haben in Dexie keine feste Reihenfolge: Sortierungen in `lib` brauchen einen letzten Tiebreaker (`id.localeCompare`), und Tests, die eine Reihenfolge prüfen, legen die Zeilen mit unterscheidbaren Sortierschlüsseln an (z. B. Fälligkeiten) statt auf die Erzeugungsreihenfolge zu bauen.
- Toasts in Komponententests: `sonner` mocken (`toast` als `vi.fn()` mit `success/error/warning`) und die `action.onClick` direkt auslösen – der echte `Toaster` braucht Pointer-Capture, das jsdom nicht hat. **Seed-Zeilen (Budget) werden mit der echten Uhr datiert** (`db/schema.ts`), auch wenn der Test `useToday`/die Repo-Uhr mockt: Tests mit fester Woche führen im Setup das Onboarding aus (wie die App) oder entfernen fremde Budgetzeilen – nie darauf bauen, wo die Seed-Zeile liegt. Gegenprobe: in `schema.ts` kurz `toISODate(new Date())` durch ein festes Datum vor/in/nach der Testwoche ersetzen → Suite muss grün bleiben.
- Test-Fixtures: `src/test/fixtures.ts` (`makeExpense`, `makeWeek`, `makeTx`, … mit festem `NOW`).
- Commits: Conventional Commits mit Feature-Scope – `feat(expenses): quick-add sheet`, `fix(lib): clamp monthly recurrence`, `test(db): closeWeek idempotency`, `chore: …`. Klein & thematisch; Logik und Test im selben Commit; vorher typecheck + lint + test grün. Branch pro Phase `phase-N-kurzname`. **Push nach `origin` ist generell freigegeben** (seit 2026-09-20); `main` wird erst nach Abnahme einer Phase per Fast-Forward aktualisiert und gepusht.

## Design

Dark = Default, Light via `[data-theme='light']`. Tokens **nur** aus `tokens.css` (keine Hex-Werte in Komponenten):
bg `#0B0C0F` · surface-1/2/3 `#14161A/#1B1E24/#252932` · fg `#F5F6F8` · fg-muted `#9AA1AE` ·
**saved/Mint `#3EE0A8`** (auch Primary-CTA) · **spent/Coral `#FF6F61`** · **income/Periwinkle `#8AA4FF`** · warning `#FFB84D` · danger `#FF5A52` · `cat-1…10` (eigene Light-Stufen, ≥ 3:1 auf Weiß) · `chart-income/-spent/-saved`, `chart-cat-1…10`, `chart-other` für Chart-Flächen.
Radien 10/14/20 (Card)/28 (Sheet) · 4-px-Raster, Seitenrand 16/32 px · Touch-Ziel ≥ 44 px · Inter Variable, Beträge immer `tabular-nums`, Display 44/48-700.
Mobile-first: BottomTabs < `lg`, Sidebar ≥ `lg`; `ResponsiveSheet` statt Modals; Shell = Grid + innerer Scroller (kein `position: fixed`), Safe-Areas, `100dvh`; Inputs ≥ 16 px; Numpad-Betrag ist kein `<input>`.
Motion: Springs aus `shared/motion.ts` (`snappy`/`soft`/`bouncy`), `whileTap scale .97`, `reducedMotion="user"`. Glas nur für Tab-Bar/Sticky-Header/Sheet-Griff.
Listen: Swipe-to-delete immer mit Undo-Toast. Ladezustände: Skeletons, nie Spinner-Seiten. Vor Chart-Code den `dataviz`-Skill laden. Screenshots von Charts mit `prefers-reduced-motion` aufnehmen – ein Ganzseiten-Screenshot ändert die Fenstergröße, Recharts animiert neu und das Bild zeigt leere Charts.
